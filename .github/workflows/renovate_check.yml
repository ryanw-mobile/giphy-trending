name: Renovate Check

on:
  merge_group:
  push:
    branches:
      - 'renovate/**'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Enable KVM group perms
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm
          ls /dev/kvm

      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Decode Keystore
        id: decode_keystore
        uses: timheuer/base64-to-file@dfa5a5dd38803cb4f5c3a081eeb28e8362d43e9c # v2.0
        with:
          fileName: 'release.jks'
          encodedString: ${{ secrets.KEYSTORE }}

      - name: set up JDK 21
        uses: actions/setup-java@be666c2fcd27ec809703dec50e508c2fdc7f6654 # v5
        with:
          distribution: 'corretto'
          java-version: '21'
          cache: gradle

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Install Xvfb
        run: sudo apt-get install -y xvfb

      - name: Debug Android Test
        run: xvfb-run ./gradlew :app:pixel2Api35DebugAndroidTest -Pandroid.testoptions.manageddevices.emulator.gpu=swiftshader_indirect -Pandroid.experimental.testOptions.managedDevices.emulator.showKernelLogging=true -Pandroid.experimental.testOptions.managedDevices.maxConcurrentDevices=1 -Pandroid.experimental.testOptions.managedDevices.setupTimeoutMinutes=180
        env:
          CI: 'true'
          GIPHYAPIKEY: ${{ secrets.GIPHYAPIKEY }}
          KEYSTORE_LOCATION: ${{ steps.decode_keystore.outputs.filePath }}  # Workaround due to api key

      - name: Gradle Check
        run: ./gradlew check assembleDebug
        env:
          CI: 'true'
          GIPHYAPIKEY: ${{ secrets.GIPHYAPIKEY }}
          KEYSTORE_LOCATION: ${{ steps.decode_keystore.outputs.filePath }}  # Workaround due to api key
