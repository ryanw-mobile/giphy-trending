package uk.ryanwong.giphytrending

class test {

    fun a() {
        val bakes = listOf("sausage roll", "steak bake", "festive bake")

// Print each item in the list
        bakes.forEach { bakeItem ->
            println(bakeItem)
        }
    }
}